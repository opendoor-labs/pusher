#!/bin/bash

brew update
brew install elixir redis
mix local.hex --force
mix archive.install --force  https://github.com/phoenixframework/phoenix/releases/download/v1.0.3/phoenix_new-1.0.3.ez
mix deps.get
npm install
