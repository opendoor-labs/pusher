#!/usr/bin/env bash

function readd-remote(){
  HAS_REMOTE=$(git remote | grep $1)
  if ! [[ -z $HAS_REMOTE ]]
  then
    git remote remove $1
  fi
  git remote add $1 $2
}
readd-remote heroku git@heroku.com:opendoor-pusher.git
readd-remote staging-heroku git@heroku.com:staging-opendoor-pusher.git
