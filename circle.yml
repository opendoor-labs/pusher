dependencies:
  pre:
    - bin/ci/install
  cache_directories:
    - ~/dependencies
    - ~/.mix
    - _build
    - deps

test:
  override:
    - bin/ci/test

deployment:
  staging:
    branch: master
    heroku:
      appname: staging-opendoor-pusher
